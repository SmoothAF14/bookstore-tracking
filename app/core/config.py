"""
config.py — Application settings (pydantic-settings).

Placeholder scaffolding. Settings model and env loading to be implemented.
"""

# TODO: Define Settings (pydantic-settings) for app, CORS, Django/delivery URLs,
#       Celery broker/backend, LLM, Google Maps (mock), and JWT auth.
# TODO: Expose a cached `settings` instance.
